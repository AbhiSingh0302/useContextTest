import Modal from '../UI/Modal';

const Cart = props => {

    return (
        <Modal onClose={props.onClose}>
            <h1>Hello guys how are you</h1>
        </Modal>
    )
}

export default Cart